<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    xmlns:f="sap.ui.layout.form"
    xmlns:core="sap.ui.core"
    class="sapUiSizeCompact"
>
    <Dialog
        id="idCreateDialog"
        title="{i18n>createDialog}"
        draggable="false"
        titleAlignment="Center"
        class="sapUiSizeCompact"
        contentWidth="25rem"
    >
        <content>
            <VBox>
                <f:SimpleForm
                    editable="true"
                    layout="ResponsiveGridLayout"
                >
                    <f:content>
                        <Label
                            text="{i18n>selectYear}"
                            required="true"
                        />
                        <ComboBox
                            id="idFyear"
                            width="100%"
                            selectedKey="{viewModel>/selectedFinancialYear}"
                            change="onComboboxChange"
                            items="{path: '/FinancialYearF4H2Set'}"
                            valueState="{viewModel>/valueState/selectedYear}"
                            valueStateText="{viewModel>/valueStateText/selectedYear}"
                        >
                            <core:ListItem
                                key="{Year}"
                                text="{Year}"
                            />
                        </ComboBox>
                    </f:content>
                </f:SimpleForm>
            </VBox>
        </content>
        <beginButton>
            <Button
                text="{i18n>okBtnText}"
                press="onYearSelect"
                type="Emphasized"
            />
        </beginButton>
        <endButton>
            <Button
                text="{i18n>cancelBtnText}"
                press="onCloseDialog"
                type="Transparent"
            />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
